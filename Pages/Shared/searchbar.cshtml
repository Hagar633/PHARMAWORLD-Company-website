<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>صفحة البحث</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        .btn-primary {
            background-color: #0d47a1; /* الأزرق الداكن */
            border-color: #0d47a1; /* نفس اللون للحدود */
        }

            .btn-primary:hover {
                background-color: #092e6a; /* درجة أغمق عند التحويم */
                border-color: #092e6a;
            }

        .dropdown-toggle {
            background-color: #0d47a1;
            border-color: #0d47a1;
            color: #fff;
        }

            .dropdown-toggle:hover {
                background-color: #092e6a;
                border-color: #092e6a;
            }
    </style>
</head>

<body dir="rtl" style="background-color: #e6f7ff;">

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-2 mt-4">
                <form method="get">
                    <div class="input-group">
                        <input type="text" name="searchQuery" class="form-control" placeholder="ادخل كلمة البحث هنا" aria-label="ابحث">
                        <div class="input-group-append">
                            <button class="btn btn-primary" type="submit">
                                <i class="fas fa-search"></i> بحث
                            </button>
                        </div>
                        <div class="input-group-append ml-3">
                            <div class="dropdown">
                                <button class="btn dropdown-toggle" type="button" id="filterDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    ترتيب
                                </button>
                                <div class="dropdown-menu" aria-labelledby="filterDropdown">
                                    <button class="dropdown-item" type="button" onclick="applyFilter('alphabetical')">ترتيب حسب الأبجدية</button>
                                    <button class="dropdown-item" type="button" onclick="applyFilter('high-to-low')">الأعلى إلى الأقل</button>
                                    <button class="dropdown-item" type="button" onclick="applyFilter('low-to-high')">الأقل إلى الأعلى</button>
                                    <div class="dropdown-divider"></div>
                                    <button class="dropdown-item" type="button" onclick="applyFilter('other')">خيارات أخرى</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        function applyFilter(filterType) {
            console.log(`تم اختيار الفلتر: ${filterType}`);
            // يمكنك هنا إضافة منطق تطبيق الفلتر
            alert(`تم تطبيق فلتر: ${filterType}`);
        }
    </script>
</body>
</html>
